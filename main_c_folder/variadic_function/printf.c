#include <stdio.h>


// print to the stdo some variables arguments 
//  the arguments should relate to conversion specifier c,s.
//  print out the % sign to the stdo using our function
// return an array of character with the exception of a null byte .



// // /////////// / // / / // / / // / / // // / //  // / / / / // / / // / / /  / /   / / / / // /

//  1. function to handle the task.
//   2. recieve arguments.  (const char *string, ...);
//    3 . check if theres any value in the function.
//     4 .  check if theres any format specifier or not.
//      5.  check the type of format specifier.
//       6. if theres a format specifier THEN check the corresponding argument.
//       7.  match the format specifeir with the argument.
/////////////////////////////////////

//      DOINGS.



while (string[i] != '\0')
{
    /* code */
    count++;
}

//    while(string)

for (int i = 0;  string[i] != '\0' i++)
{
    /* code */


}

{
    string[i];
    printf("miriam is a boy");

    switch (string)
    {
    case 'd' witde va_arg(arg, );
        /* code */

        break;

        default:
        break;
    }
    // va_arg(arg, char);
}

//    3.  if theres no value throw an error.
//       ans = if(string == NULL);
//    4. if theres no format specifier;
//     ans write out to the standoutput.
//    5.  

int main(int argc, char** argv)
{


}