/*Write a function that returns the length of a string.*/
#include <stdio.h>
#include "main.h"

int _strlen(char *s)
{
    int i = 0;

    while (s[i] != '\0')
    {
        i++;
        /* code */
    }

    return i;
}